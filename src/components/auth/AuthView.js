import React, { useRef, useState, useContext } from "react"
import { useHistory } from "react-router-dom"
import "./AuthView.css"

export const AuthView = props => {
    return (
        <main className="auth__container">
            Yo, you made it to the login.
        </main>
    )
}